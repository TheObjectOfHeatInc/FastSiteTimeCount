services:
  timer-app:
    build: .
    container_name: telegram-timer
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./images:/app/images
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/time"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s